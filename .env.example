# Optional local environment variables for extension exercises.
# The base repository works without a `.env` file.
# If you add an external API or protected test account, copy this file to `.env`
# and put real values only in that local file.

API_TOKEN=REPLACE_ME
STAGING_BASE_URL=https://example.invalid
TEST_PASSWORD=REPLACE_ME
